# Development and Testing Dependencies
# For production service dependencies, see each service's requirements.in file:
#   - ingress/requirements.in
#   - auth/requirements.in
#   - classifier/requirements.in
#   - gateway/requirements.in

# Linting (make lint-python, make lint-fix)
ruff>=0.14.0

# Test scripts (scripts/send_webhook.sh, scripts/test-correlation-tracing.sh, scripts/test-dev-environment.sh)
python-jose[cryptography]>=3.3.0
websockets>=12.0
